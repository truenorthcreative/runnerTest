---
   - name: Install Common packages
     hosts: ALL
     remote_user: root
     gather_facts: False
     connection: local
     tasks:
        - name: Install bind-utils
          shell: mkdir /tmp/mohiuddin